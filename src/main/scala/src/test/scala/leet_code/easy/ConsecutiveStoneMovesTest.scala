package leet_code.easy

class ConsecutiveStoneMovesTest extends FunSuite {
  test("Consecutive stone moves returns the correct result for correct input") {
    assert(() === "")
  }

  test("If bounds are too close, the function returns the lower bound value") {
    assert(ConsecutiveStoneMoves.numMovesStones(1, 2, 3)(0) === 0)
  }
}
