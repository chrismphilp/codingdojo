#include "AddTwoNumbers.h"

ListNode* AddTwoNumbersTest::addTwoNumbers(ListNode* l1, ListNode* l2) {
    ListNode* l3;
    int l1Base = 1;
    int l2Base = 1;

    while (l1->next != nullptr && l2->next != nullptr) {
        if (true) {

        }
    }
    return l3;
}
